# Add content and extraction script

Commit: Add content extraction helper for the STgeorge project.

Files added:
- scripts/extract_content.js
- scripts/INSTRUCTIONS.md

Branch: add-content-and-agpeya

Purpose:
- Provide an automated, repeatable way to extract the zipped content already in the repository into the project tree so the app can use the JSON files locally.

Next action:
- If you want me to continue and modify the app source (STgeorge-main) to wire the JSON data into the UI, reply here with "Proceed modify app" and I'll update the app files on the same branch and open a PR when ready.
